meta {
  name: Generar secreto y QR
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/2fa/generate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

settings {
  encodeUrl: true
}

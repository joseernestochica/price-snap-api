meta {
  name: Verificar token
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/2fa/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

body:json {
  {
      "token": "196450"
  }
}

settings {
  encodeUrl: true
}
